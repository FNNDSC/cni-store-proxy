const axios = require('axios');
const print = require('./print');
const colors = require('colors');

/**
 * Authorize with the ChRIS_store and discover the
 * evaluation feed ID.
 *
 * @param req request
 */
function getEvaluation(req) {

}

/*
 * First, check if user owns the plugin they are requesting for
 * GET /api/v1/ .collection_links.owned_plugin_metas -->
 * https://chrisstore.co/api/v1/users/2/ownedpluginmetas/
 */

module.exports = (req, res) => {

}
